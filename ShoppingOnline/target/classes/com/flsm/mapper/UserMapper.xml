<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flsm.mapper.UserMapper">
	<select id="login" resultType="com.flsm.pojo.users">
		select * from users where uname=#{zhanghao} or uzname=#{zhanghao} or email=#{zhanghao} and pwd=#{pwd}
	</select>
</mapper>